<%= form_with( url: upload_path, multipart: true)  do |form| %>

  Only JSON files
  <%= form.file_field :file, accept: 'application/json',  class: "form-control"%>
  <%= form.submit t('.save'), class: "btn primary-btn" %>
  <% end %>
