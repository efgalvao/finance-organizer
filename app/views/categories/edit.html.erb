<div class="title">
  <%= @category.name %>
</div>

<div class="form">

  <%= render 'form', category: Category.find(params[:id]) %>

  <%= link_to 'Back', categories_path, :class => "btn btn-info" %>
</div>
