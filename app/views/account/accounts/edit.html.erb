<div class="title">
  <%= @account.name %>
</div>

<div class="form">
<div class="form">
<%= form_with(url: account_path(@account), method: :patch, scope: :account, local: true) do |form| %>
  <% if @account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
        <% @account.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <form>
    <div class="form-group">
      <%= form.label :name, class: "input-group-text" %>
      <%= form.text_field :name, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= form.label "Savings account ?", class: "input-group-text" %>
      <%= form.check_box :savings, class: "form-control" %>
    </div>

    <div class="form-group">
      <%= form.submit class: "btn btn-success" %>
    </div>
  </form>
<% end %>

<%= link_to 'Back', :back, class: "btn btn-primary" %>
</div>
