<% content_for :title, @account.name %>

<div class="title">
  <%= @account.name %>
</div>

<% if @account.broker? %>
  <%= render 'stocks' %>
  <%= render 'stocks_table' %>
<% else %>
  <%= render 'savings' %>
<% end %>
